#include <stdio.h>
#include <stdlib.h>
#include "struct_function.h"
#include <string.h>
#include <stdio.h>
#include <stdlib.h>


struct Book{
char genre[20];
char author[25];
};


int change_genre(struct Book * pbook){

char text[20];

puts("Please enter book genre:");
gets(text);
//if(strcmp( text , "xaxa") == 0)


return 0;
}
